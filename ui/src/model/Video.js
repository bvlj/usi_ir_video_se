export default class Video {

    constructor(id, title, author, category, source) {
        this.id = id;
        this.title = title;
        this.author = author;
        this.category = category;
        this.source = source;
    }
}